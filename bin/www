#!/usr/bin/env node
var debug = require('debug')('MeanBlogEngine');
var app = require('../app');

app.set('port', process.env.PORT || 3003);

var server = app.listen(process.env.PORT, process.env.IP);

